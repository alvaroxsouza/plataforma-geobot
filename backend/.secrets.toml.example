# Arquivo de segredos local - NÃO COMMITAR
# Copie este arquivo para .secrets.local.toml e configure seus valores

[default]
# Chave secreta para JWT - GERAR UMA CHAVE FORTE E ÚNICA
# Para gerar uma chave segura, use: python -c "import secrets; print(secrets.token_urlsafe(32))"
secret_key = "CHANGE-ME-TO-A-SECURE-RANDOM-KEY"

# Senha do banco de dados
db_password = "sua_senha_aqui"

[development]
secret_key = "dev-secret-key-CHANGE-IN-PRODUCTION"
db_password = "postgres"

[production]
# IMPORTANTE: Use valores seguros em produção
secret_key = "production-secret-key-MUST-BE-CHANGED"
db_password = "secure_password_here"

